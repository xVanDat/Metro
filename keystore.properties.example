# Copy this file to keystore.properties and replace the placeholder values.
# The real file and keystore are ignored by Git.
storeFile=keys/metro-release.jks
storePassword=change-me
keyAlias=metro
keyPassword=change-me
